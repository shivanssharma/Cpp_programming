#Syntax: matrix(data,nrow,ncol,byrow,dimnames)
        #data = data
        #nrow = number of rows
        #ncols = number of columns
        #byrow = ????
        #dimnames = name of the row and column

mtr = matrix(c(6:30),5,5)
#c(6:30) means its a sequence of number starting form 6 to 30 total 25 number (6,7,8....,30)
#View(mtr)
